<?php
// collector_dashboard.php
// Simple Collector Dashboard with modules:
// Explore Artworks, My Purchases, Favorites, Transactions, Settings

session_start();

// --- Mock authentication check ---
if (!isset($_SESSION['user_role']) || $_SESSION['user_role'] !== 'collector') {
    // In real app, redirect to login
    $_SESSION['user_role'] = 'collector'; // demo fallback
}

// Determine active module
$module = $_GET['module'] ?? 'explore';

function isActive($key, $module) {
    return $key === $module ? 'active' : '';
}
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Collector Dashboard</title>
    <style>
        body { margin:0; font-family: Arial, Helvetica, sans-serif; background:#0f172a; color:#e5e7eb; }
        .layout { display:flex; min-height:100vh; }
        .sidebar { width:240px; background:#020617; padding:20px; box-shadow: 2px 0 10px rgba(0,0,0,.4); }
        .brand { font-size:20px; font-weight:700; margin-bottom:24px; color:#a78bfa; }
        .nav a { display:block; padding:12px 14px; margin:6px 0; border-radius:10px; color:#cbd5f5; text-decoration:none; }
        .nav a:hover { background:#111827; }
        .nav a.active { background:#4f46e5; color:white; }
        .content { flex:1; padding:28px; }
        .card { background:#020617; border:1px solid #1f2937; border-radius:16px; padding:20px; box-shadow: 0 10px 20px rgba(0,0,0,.35); }
        .grid { display:grid; grid-template-columns: repeat(3, 1fr); gap:16px; }
        .art { background:#0b1020; border:1px solid #1f2937; border-radius:14px; padding:14px; }
        .btn { padding:8px 12px; border-radius:10px; border:1px solid #374151; background:#111827; color:#e5e7eb; cursor:pointer; }
        .btn.primary { background:#4f46e5; border-color:#4f46e5; }
        table { width:100%; border-collapse: collapse; }
        th, td { padding:10px; border-bottom:1px solid #1f2937; text-align:left; }
        .header { display:flex; align-items:center; justify-content:space-between; margin-bottom:18px; }
        .badge { background:#111827; border:1px solid #1f2937; padding:4px 8px; border-radius:999px; }
    </style>
</head>
<body>
<div class="layout">
    <aside class="sidebar">
        <div class="brand">ARTLAB • Collector</div>
        <nav class="nav">
            <a class="<?= isActive('explore', $module) ?>" href="?module=explore">Explore Artworks</a>
            <a class="<?= isActive('purchases', $module) ?>" href="?module=purchases">My Purchases</a>
            <a class="<?= isActive('favorites', $module) ?>" href="?module=favorites">Favorites</a>
            <a class="<?= isActive('transactions', $module) ?>" href="?module=transactions">Transactions</a>
            <a class="<?= isActive('settings', $module) ?>" href="?module=settings">Settings</a>
        </nav>
    </aside>

    <main class="content">
        <?php if ($module === 'explore'): ?>
            <div class="header">
                <h2>Explore Artworks</h2>
                <span class="badge">Discover new art</span>
            </div>
            <div class="card">
                <div class="grid">
                    <?php for ($i=1; $i<=6; $i++): ?>
                        <div class="art">
                            <div style="height:120px; background:#111827; border-radius:10px; margin-bottom:10px;"></div>
                            <strong>Artwork #<?= $i ?></strong>
                            <p style="margin:6px 0 10px; color:#9ca3af;">Artist Name</p>
                            <button class="btn primary">View</button>
                            <button class="btn">Favorite</button>
                        </div>
                    <?php endfor; ?>
                </div>
            </div>
        <?php elseif ($module === 'purchases'): ?>
            <div class="header">
                <h2>My Purchases</h2>
                <span class="badge">Owned artworks</span>
            </div>
            <div class="card">
                <table>
                    <thead>
                        <tr><th>Artwork</th><th>Artist</th><th>Date</th><th>Price</th></tr>
                    </thead>
                    <tbody>
                        <tr><td>Sunset Dreams</td><td>J. Cruz</td><td>2026-01-15</td><td>₱5,000</td></tr>
                        <tr><td>Ocean Whispers</td><td>M. Reyes</td><td>2026-01-22</td><td>₱7,500</td></tr>
                    </tbody>
                </table>
            </div>
        <?php elseif ($module === 'favorites'): ?>
            <div class="header">
                <h2>Favorites</h2>
                <span class="badge">Saved artworks</span>
            </div>
            <div class="card">
                <ul>
                    <li>Golden Horizon — by A. Santos</li>
                    <li>Neon City — by L. Garcia</li>
                </ul>
            </div>
        <?php elseif ($module === 'transactions'): ?>
            <div class="header">
                <h2>Transactions</h2>
                <span class="badge">Payment history</span>
            </div>
            <div class="card">
                <table>
                    <thead>
                        <tr><th>ID</th><th>Type</th><th>Date</th><th>Status</th><th>Amount</th></tr>
                    </thead>
                    <tbody>
                        <tr><td>#TX1021</td><td>Purchase</td><td>2026-01-15</td><td>Completed</td><td>₱5,000</td></tr>
                        <tr><td>#TX1033</td><td>Refund</td><td>2026-02-02</td><td>Processed</td><td>₱1,200</td></tr>
                    </tbody>
                </table>
            </div>
        <?php elseif ($module === 'settings'): ?>
            <div class="header">
                <h2>Settings</h2>
                <span class="badge">Account preferences</span>
            </div>
            <div class="card">
                <form method="post" action="save_settings.php">
                    <label>Display Name</label><br>
                    <input type="text" name="display_name" value="Collector" style="width:100%; padding:10px; margin:6px 0 12px; background:#0b1020; border:1px solid #1f2937; color:#e5e7eb; border-radius:10px;"><br>
                    <label>Email Notifications</label><br>
                    <select name="notifications" style="width:100%; padding:10px; margin:6px 0 12px; background:#0b1020; border:1px solid #1f2937; color:#e5e7eb; border-radius:10px;">
                        <option value="on">On</option>
                        <option value="off">Off</option>
                    </select>
                    <button class="btn primary" type="submit">Save Changes</button>
                </form>
            </div>
        <?php endif; ?>
    </main>
</div>
</body>
</html>
